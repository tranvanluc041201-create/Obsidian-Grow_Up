---
title: "2.3 指针自增优先级"
difficulty: L2
frequency: 首次
repeated: false
is_hard: false
last_review: 2026-02-19
next_review: 2026-02-20
tags: [内存指针, 运算符优先级, 自增, 右结合]
related_note: "[[03-pointers-null]]"
type: 编程错题
language: C
module: 内存地址
created: 2026-02-19
---
# 2.3 指针自增优先级

## 🎯 题目 / 场景

指针自增运算优先级混淆：

```c
int b = *p++;  // ❓ 意图是数值+1，但...
```

**实际结果**：先取值，然后指针后移（不是数值+1！）

---

## 💥 发生了什么

- **意图**：让指针指向的数值加1
- **实际**：取当前值，然后指针指向下一个位置
- **等价写法**：`*(p++)`

---

## 🤔 我当时怎么想的

> "`*p`是取值，`++`是自增，合起来就是让值自增..."
> **盲区**：不知道后缀`++`优先级高于`*`，且`p++`是**先返回旧值，再自增指针**。

---

## 🔍 根因分析

### 表面原因
> 混淆了`*p++`和`(*p)++`的含义

### 深层原理

**优先级与结合性**：
| 表达式 | 等价写法 | 结果 |
|--------|---------|------|
| `*p++` | `*(p++)` | 取`*p`的值，然后`p`后移 |
| `(*p)++` | — | 取`*p`的值，然后`*p`自增 |
| `*++p` | `*(++p)` | `p`先前移，然后取`*p` |
| `++*p` | `++(*p)` | `*p`先自增，然后取值 |

**关键区别**：
- `p++`：表达式值是旧地址，p自身增加
- `++p`：表达式值是新地址，p自身增加

### 识别信号
> **指针+自增的4种写法含义完全不同**，必须加括号明确意图。

---

## ✅ 正确规则与修正

```c
// ❌ 错误意图：想让值+1
int b = *p++;  // 实际：取值，指针后移

// ✅ 正确写法1：明确让值+1
int b = (*p)++;  // 取*p的值，然后*p自增

// ✅ 正确写法2：拆成两行（推荐！清晰）
int b = *p;  // 取值
(*p)++;      // 值自增

// ✅ 正确写法3：前置自增
++*p;  // 或 (*p)++，但++*p更简洁
```

---

## 🔁 一题多变 / 横向关联（视情况补充）

### 同类变式
- **更复杂的场景**
  ```c
  int arr[] = {10, 20, 30};
  int *p = arr;
  
  printf("%d\n", *p++);   // 输出10，p指向arr[1]
  printf("%d\n", *p);     // 输出20
  ```

### 横向关联
- **CS50笔记**[[10_编程/笔记/cs50-week4-memory-notes/03-pointers-null]]
- **关联错题**：[[2.8] 指针优先级]（同类问题，更详细）

### 工程启示
> **代码可读性优先**：
> - 一行只写一个副作用
> - 复杂表达式拆成多行
> - 用括号明确意图，不要依赖记忆优先级

---

### 怎么自检

- [ ] 我能解释每一处修改的原因
- [ ] 我能手写出最小正确版本
- [ ] 我能说出同类题的触发信号

## 🔔 复习提醒

- [ ] 立即复盘（理解正确做法） 📅 2026-02-19
- [ ] 快速回顾（遮住答案，能否说出坑点） 📅 2026-02-20
- [ ] 代码复现（手写4种变式） 📅 2026-02-22
- [ ] 考前速览（只看思维盲区） 📅 2026-03-06

---

## 📊 复习记录

| 日期 | 操作 | 掌握度 | 备注 |
|------|------|--------|------|
| 2026-02-19 | 录入 | ⭐⭐⭐ | L2较简单，已掌握 |

---

## 🎯 关键记忆点

| 表达式 | 口诀 |
|--------|------|
| `*p++` | 先取值，指针后移 |
| `(*p)++` | 取值并自增，指针不动 |
| `*++p` | 指针先移，再取值 |
| `++*p` | 值先自增，再取值 |




