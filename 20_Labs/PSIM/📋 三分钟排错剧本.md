
---



### Step 0（30s）：打开强制观测窗（同屏）
必看信号 [1]：
- `Vout1/2/3`, `Vfb1/2/3`, `e1/2/3`, `PIout`, `Limout`, `ramp`, `PWM1/2/3`, `IL`

**判据**：如果看不到 `d vs ramp` 与 `PWM` 的对应关系，就别改电路，先把观测补齐

### Step 1（60s）：看 Limout 是否卡死
1. 检查 `Limout` 是否在 0..1 **且不长期贴边** [1]
2. 检查 `ramp` 是否 0..1 且频率正确（50kHz）
3. 检查 `Comparator` 极性：`ramp < d → PWM=1` [1]

**如果卡死**：先查误差极性、Limiter 范围、ramp 范围一致性

### Step 2（60s）：做"参考点硬拉"验证
1. 把关键传感器负端直接拉到 VDC− 黑点节点 [1]
2. 观察输出是否立刻恢复

**如果立刻正常**：根因 = 参考点不一致，统一所有地后 END

**如果仍异常**：进入 Step 3

### Step 3（30s）：确认 Vgs/开关真导通
1. 直接测 $V_{gs} = V_{gate} - V_{source}$ 是否足够 [1]
2. 或改用 Controlled/Ideal Switch 做对照

**如果 Vgs 太小**：根因 = 浮地驱动，改模型或做高边驱动参考

---

## ✅ Checklist

### 搭建前（1 分钟）
- [ ] 三路输出的测量点都在各自 $C_{out}$ 两端 [1]
- [ ] ramp = 50kHz 且幅值 0~1 [1]
- [ ] Limiter 输出范围 = 0~1 [1]
- [ ] 每路 $C_{out}$、$R_{load}$ 的连接方向与参考点清晰 [1]
- [ ] 续流路径在 S1 OFF 时存在且方向正确 [1]

### 仿真前（1 分钟）
- [ ] 把 PI 先断开，手动给 $d = 0.2/0.5/0.8$，确认 PWM 占空比真的随 $d$ 变化 [1]
- [ ] 选通器件的驱动参考正确（尤其 MOSFET：确认 Vgs） [1]
- [ ] 三路共用 ramp ✅
- [ ] Comparator 极性用"固定 $d$"验证一次 [1]

### 出错时（3 分钟，按优先级）
- [ ] Limout 是否卡死 0/1？ [1]
- [ ] 地是否真同节点？（传感器负端拉到 VDC− 黑点） [1]
- [ ] Vgs 是否成立？（Gate-Source） [1]
- [ ] 量纲/极性是否匹配？（ramp、Limiter、e 的范围与符号）
- [ ] 最后才：PI 参数/启动策略 [1]

### 命名规范（非常省命）
- [ ] 参考点统一命名：`GND_PWR`（功率地）、`GND_CTRL`（控制地）
- [ ] 控制量统一量纲：`e`（伏）、`PIout/VC/d`（0..1）、`ramp`（0..1）
- [ ] 探针命名固定：`VC1/2/3`, `PWM1/2/3`, `Vgs_S1`, `Vgs_S2a/b`, `IL`

---

## 📇 可复用模块库（下次搭建时直接拿）

### 模块 1：Error + PI + Limiter（每路一个）
- **输入**：$V_{ref,i}$, $V_{fb,i}$
- **输出**：$d_i$（0..1）
- **判据**：稳态时 $d_i$ 在 0..1 中间某个值，不应长期卡死边界 [1]

### 模块 2：Ramp + Comparator → PWM
- **输入**：$d_i$（0..1）、ramp（0..1）
- **输出**：$PWM_i$
- **判据**：$d_i$ 越大，PWM 高电平占比越大

### 模块 3：选通/开关驱动
- **输入**：$PWM_i$
- **输出**：支路导通状态
- **注意**：若用 MOSFET，高边/浮地必须确保 Vgs；否则用 Controlled/Ideal Switch [1]

---

## ❓ 常见问题与 PSIM 器件选型

### Q1：PSIM 里"红线/绿线"是什么意思？

在 PSIM 中：
- **Power 网络（红线）**：承载功率电流/电压的电气连接
- **Control/Logic 网络（绿线）**：承载控制信号（0/1、PWM、比较器输出）

你说的"从 on-off switch 出来就是红色，改不了"，说明：**你选的 on-off switch 是"功率器件"而不是"逻辑控制元件"**，它输出不是绿线，所以没法当 PWM 控制信号

### Q2：On-Off Switch vs Bi-directional Switch 的区别？

| 器件 | 用途 | 控制方式 | 适用场景 |
|---|---|---|---|
| **On-Off Switch** | 开/关某条功率支路或测试隔离 | 不一定有控制端口（绿线） | 手动/固定隔离，**不适合 PWM 控制** |
| **Bi-directional Switch** | 功率级开关器件，可双向导通 | 需配套 Switch Controller 受控 | **支路选通/背靠背 MOSFET 等效** |

### Q3：你现在应该用哪个？

**答案**：用 **Bi-directional Switch**（功率开关）+ **Switch Controller**（控制器）

**理由**：你需要"受 PWM 控制的功率路径选通"，而不是"手动开关"

**PSIM 操作步骤**：
1. 功率器件：**Power → Switches → Bi-directional Switch**
2. 控制器件：同一 Library Browser 左侧 → **Switch Controllers**
3. 用你的 PWM/逻辑信号（绿线）去驱动控制器，再由控制器去驱动 Bi-directional Switch

---

## 🎓 核心概念速记

### PWM 的本质
**脉宽调制** = 用"开关开多久"来控制平均能量 [1]

### 闭环 vs 开环
- **开环**：固定 $D$，系统不管输出变没变（蒙眼开车）
- **闭环**：实时测 $V_{fb}$ 和目标 $V_{ref}$ 比较，自动调 $D$（看着车道线纠偏）

### SITO 三输出共电感的关键
**不是 PI 怎么算**，而是**"电感能量在某时刻主要喂给哪一路"由支路选通器件决定** [1]

---

## 💡 最后一句话

**电压必须有参照**。没有同一参考点，PWM、Vgs、VC 全都"看起来像对的"，但本质是假的 [1]

下次搭建时，第一步就统一所有地和参考点。能省你 80% 的人生。

---

<br/>
<details class="copilot-sources"><summary class="copilot-sources__summary">Sources</summary>
<ul class="copilot-sources__list">
<li class="copilot-sources__item"><span class="copilot-sources__index">[1]</span><span class="copilot-sources__text"><a href="obsidian://open?file=20_Labs%2FPSIM%2FSITO-3out-Buck-%E9%97%AD%E7%8E%AF%E5%A4%8D%E7%8E%B0-%E5%A4%8D%E7%9B%98.md.md">SITO-3out-Buck-闭环复现-复盘.md</a></span></li>
</ul>
</details>