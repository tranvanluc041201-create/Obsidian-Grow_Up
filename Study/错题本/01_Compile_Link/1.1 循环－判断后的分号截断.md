### [1.1] 循环/判断后的分号截断
* **标签**：#Syntax #ControlFlow
* **错误**：`for(int i=0; i<10; i++); { ... }`
* **根因**：分号代表“空语句”。循环体空转 10 次后，才执行后面的代码块（只执行 1 次）。
* 
* 类似while(i!=10)i++;(初始化i=0)__可以用这种"错误"对i累加
* 
* **修正**：删掉 `)` 后面的分号。

