for (i=0;i<3;i++){
printf("%d %5.1f\n%5d %s %5.1f %5.1f %5.1f %5.1f\n", 
i,
 average[i],
 student[pos[i]].number,
  student[pos[i]].name,
   student[pos[i]].score1,
   student[pos[i]].score2,
    student[pos[i]].score3,
student[pos[i]].average);
// 问题所在,score你定义的都是int型,而老师要求输出是float型(输出时float会转换为double型)
//类型不匹配，int读取4字节，double读取8字节，剩余的四字节电脑会存入垃圾数据