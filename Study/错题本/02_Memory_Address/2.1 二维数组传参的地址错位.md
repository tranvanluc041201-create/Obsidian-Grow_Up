### [2.1] 二维数组传参的地址错位
* **标签**：#Memory #Stride #Array
* **错误**：Main: `int a[20][20]`; Func: `void f(int a[][3])`
* **根因**：**内存步长不匹配**。主函数跳一行跨 20 个数，子函数跨 3 个数，访问位置错乱。
* **修正**：维度必须严格一致 `void f(int a[][20])`。

