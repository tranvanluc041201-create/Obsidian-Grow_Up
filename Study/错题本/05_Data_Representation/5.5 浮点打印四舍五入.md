### [5.5] 浮点打印四舍五入
* **标签**：#Float #Rounding
* **错误**：以为 `printf("%.2f")` 只是截断。
* **原题**：
```c
float b = 9.8765;
printf("%.2f", b);
```
* **正确结果**：`9.88`。
* **根因**：`printf` 按舍入规则输出，小数位显示通常会四舍五入；浮点数本身也存在表示误差。
**总结**:强制转换时才会直接截断
