## 1）总览地图：这节课到底在讲什么（主线）
- **字符串**：C 的字符串 = `char` 数组 + 结尾 `'\0'`；很多时候用 `char *` 指向首字符
- **动态内存**：`malloc` 向堆要内存,失败返回 `NULL`；用完 `free`
- **错误类型**：越界读/写、未初始化、泄漏（Valgrind 最常抓这三类）
- **文件 I/O**：RAM 是易失的；要持久化就写到磁盘文件（"w" 覆盖 / "a" 追加 / "r" 读取）

---