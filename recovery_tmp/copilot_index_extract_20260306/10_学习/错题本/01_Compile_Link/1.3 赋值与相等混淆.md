### [1.3] 赋值与相等混淆 (The "=" vs "==" Trap)
* **标签**：#Logic #Classic
* **错误**：`if (x = 0) { ... }`
* **根因**：`x = 0` 是赋值表达式，结果为 0 (假)。导致判断永远失效，且 x 被篡改为 0。
* **修正**：`if (x == 0)`。建议习惯写成 `if (0 == x)` (常量在左)。
* 习惯:=说赋值,==说等于;