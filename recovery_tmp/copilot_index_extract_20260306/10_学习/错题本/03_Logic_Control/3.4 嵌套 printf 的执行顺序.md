### [3.4] 嵌套 printf 的执行顺序
* **标签**：#Logic #IO
* **错误**：只盯返回值，忽略内层 `printf` 的副作用和求值顺序。
* **原题**：
```c
int x = printf("%d", printf("Error"));
printf("%d", x);
```
* **正确结果**：屏幕输出 `Error51`，`x` 为 1。
* **根因**：外层 `printf` 需要内层的返回值作为参数，内层必须先执行；`printf` **返回已打印字符数**。