### [3.5] 短路导致的计数丢失
* **标签**：#Logic #ShortCircuit
* **错误**：把 `&&` 右侧当成一定执行。
* **原题**：
```c
int m = 1, n = 1;
int y = (--m) && (++n);
```
* **正确结果**：`m=0, n=1, y=0`。
* **根因**：`&&` 左侧为 `0` 时短路，右侧表达式不会执行。
* **修正**：避免在逻辑表达式里放副作用；必要时拆成多行。