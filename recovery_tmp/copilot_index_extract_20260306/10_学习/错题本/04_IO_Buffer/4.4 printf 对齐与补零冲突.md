### [4.4] printf 对齐与补零冲突
* **标签**：#IO #Format
* **错误**：认为 `%-04d` 既左对齐又补零。
* **原题**：
```c
int val = 7;
printf("|%-04d|", val);
```
* **正确结果**：`|7   |`。
* **根因**：`-` 表示左对齐，会覆盖 `0` 的补零规则,即"-"的优先级>"0"的优先级；左对齐时只能补空格。
* **修正**：需要补零用 `%04d`；需要左对齐用 `%-4d`，两者不可同时生效。