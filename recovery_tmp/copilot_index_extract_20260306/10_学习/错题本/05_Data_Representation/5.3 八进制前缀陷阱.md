### [5.3] 八进制前缀陷阱 (Octal Trap)
* **标签**：#Number #Literal
* **错误**：把带 `0` 前缀的整数字面量当作十进制。
* **原题**：
```c
int k = 017; // 注意这个 0 前缀
printf("%d, %o, %x", k, k, k);
```
* **正确结果**：`15, 17, f`。
* **根因**：C 语言中 `017` 是八进制，等价于十进制 `15`
**总结**:"0"为八进制前缀,"0x"为十六进制前缀